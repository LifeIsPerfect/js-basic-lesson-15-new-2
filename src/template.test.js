const { f1, f2, f3 } = require( './homework' );

describe( 'Homework #N, Task #1', () => {
    it( 'Case #1', () => {
        expect( 3 ).toBe( 3 );
    } );

    it( 'Case #2', () => {
        expect( 3 ).toBe( 3 );
    } );
} );

describe( 'Homework #N, Task #2', () => {
    it( 'Case #1', () => {
        expect( 3 ).toBe( 3 );
    } );

    it( 'Case #2', () => {
        expect( 3 ).toBe( 3 );
    } );
} );

describe( 'Homework #N, Task #3', () => {
    it( 'Case #1', () => {
        expect( 3 ).toBe( 3 );
    } );

    it( 'Case #2', () => {
        expect( 3 ).toBe( 3 );
    } );
} );